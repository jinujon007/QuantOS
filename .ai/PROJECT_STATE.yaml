# Machine-readable current state. Actively maintained only — no history.
# Update at the close of every work package. See README.md for precedence rule:
# repository evidence (code, tests, CI, git log, docs/) overrides this file.

project: QuantOS
description: Institutional-grade automated trading platform for the Indian equity market
status: active_development
solo_operator: true

architecture_status: frozen
constitution_status: frozen
roadmap_status: frozen  # Phases 0-9, per Blueprint

current_phase:
  number: 2
  name: Data Platform
  scope: DataProvider port, point-in-time universe store, corporate actions, quality validation (closes F1/F9)
  code_status: in_progress  # WP-007 opened Phase 2: ports + PIT universe store + fail-closed price provider
  phase_1_status: fully_closed  # WP-001..005 complete (WP-006 reserved, deferred)
  work_package_filed: false     # next: corporate actions / quality validators / fetch adapter, or WP-006

modules:
  config:
    status: partial  # WP-002: environment resolution + typed/validated/immutable AppConfig, no file I/O yet
    fields: [environment]
    layered_config: not_implemented  # reserved as WP-006
  storage:
    status: partial  # WP-003: Repository[T] port + Entity + SqliteRepository; no domain aggregates, no Parquet
    adapters: [sqlite]
  utils:
    status: partial  # WP-004 structured JSON-lines logging; WP-018 NSE session helpers (exchange-calendars XBOM, ADR-044)
  data:
    status: partial  # WP-007 ports/PIT store/CSV provider; WP-018 bhavcopy adapter (ADR-044, bhavcopy-primary, yfinance quarantined)
    pit_universe_snapshots_since: "2026-07-14"
    bhavcopy_raw_archive_since: "2026-07-21"  # data/bhavcopy/, immutable, gitignored (regenerable from NSE archive)
  brokers:
    status: partial  # WP-008/ADR-034: ports + Paper/Zerodha/Angel adapters; reconciliation+heartbeats = Phase 6/8
    adapters: [paper, zerodha_kite, angel_smartapi]
  risk:
    status: partial  # WP-008: persisted kill switch + KillSwitchGate; full Part V control table = Phase 4
  execution:
    status: partial  # WP-008: gated engine + order journal; lifecycle/reconciliation/slippage = Phase 6
  factors:
    status: partial  # WP-009: momentum_12m1m + regime, verbatim ports, parity-proven
  strategies:
    status: partial  # WP-009: Strategy port + MomentumV1 + registry loader; stop-loss carry = portfolio module
    registry: strategies_registry/momentum_v1.yaml  # editing restarts the validation clock

demo:
  entrypoint: tools/demo_pipeline.py
  broker_probe: tools/broker_connect_check.py
  last_run: "2026-07-14"  # runs the real Momentum v1.0 (WP-009), both regimes replayed

operator_console:
  generator: tools/build_dashboard.py   # --open to launch browser; output dashboard/index.html (git-ignored)
  kill_switch_cli: tools/kill_switch.py # status | engage "reason" | release "reason" -> data/risk.db
  adr: ADR-035  # static read-only precursor to Phase 9; browser-verified 2026-07-14

last_completed_work_package:
  id: WP-018
  name: Bhavcopy-Primary Fetch Adapter + NSE Trading Calendar
  date: "2026-07-21"
  phase: 2  # ADR-044 (operator-approved); same day: WP-017 = ADR-042, ADR-043, TD-012/015 closed

ci_gates:
  legacy_scripts_mypy: informational        # 7 pre-existing errors, tracked, not blocking
  quantos_core_mypy_strict: blocking         # WP-001; now covers 22 source files
  quantos_core_import_smoke_test: blocking   # WP-001
  import_boundary_enforcement: blocking      # WP-005: ADR-032 matrix via tests/quantos_core/test_import_boundaries.py

reserved_work_packages:
  - id: WP-006
    name: Layered Configuration
    status: reserved_not_specified

governing_documents:
  constitution: 'docs/00_governance/(AI) QuantOS Constitution.md'
  blueprint: 'docs/02_architecture/(AI) QuantOS Target Architecture Blueprint - 2026-07-13.html'
  independent_audit: 'docs/01_audits/(AI) QuantOS Independent Audit - 2026-07-13.html'
  external_due_diligence: 'docs/01_audits/(AI) QuantOS External Repository Due Diligence - 2026-07-13.html'
  oss_adoption_review: 'docs/01_audits/(AI) QuantOS OSS Adoption Review - 2026-07-13.html'
  adrs_dir: 'docs/adr/'
  risk_register: 'docs/00_governance/Risk Register.md'
  tech_debt_register: 'docs/00_governance/Technical Debt Register.md'

metrics:
  digital_readiness_score: "61/100"  # CONTEXT.md DRS re-score 2026-07-13; independent DD 2026-07-21 concurs
  test_coverage_pct: 27              # quantos_core real code at 100%; repo total incl. legacy scripts
  test_count: 282                    # verified 2026-07-21 post-WP-018 (offline suite)

open_items:
  risks_total: 5
  risks_mitigated: 1
  tech_debt_total: 18
  tech_debt_resolved: 7  # TD-007, TD-009..012, TD-015, TD-016 (TD-012 + TD-015 closed 2026-07-21 maintenance)

repository:
  default_branch: main
  license: proprietary-all-rights-reserved
  remote: https://github.com/jinujon007/QuantOS
  visibility: private

modules_scaffolded_empty: 4  # analytics, live, monitoring, validation (init-only); recount 2026-07-21 (TD-012 fix)

last_updated: "2026-07-21"
