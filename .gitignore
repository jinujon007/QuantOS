# Python
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# Virtual environment (1GB+, machine-specific, superseded by pyproject.toml + lockfile)
venv/
.venv/

# Real secrets — never committed, .env.example is the template
.env

# Tool-specific telemetry/cache — not source
.gstack/

# OS
Thumbs.db
.DS_Store

# Build artifacts & local runtime state (regenerable; risk.db is operational state, not source)
dashboard/index.html
data/demo/
data/risk.db
data/daily_run.log
