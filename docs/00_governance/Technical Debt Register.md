---
type: register
date: 2026-07-14
status: active
updated_by: Program audit 2026-07-14
---

# Technical Debt Register

Living document. Updated at the end of every work package (never silently)
— items added when discovered, closed with the work package that resolved
them, never deleted from history (mark `Resolved`, keep the row).

| ID | Item | Severity | Impact | Discovered | Resolution phase | Est. effort |
|---|---|---|---|---|---|---|
| TD-001 | No per-trade log artifact anywhere in the codebase — `run_backtest()` returns only an equity curve, no trade-by-trade record | Medium | Can't audit individual trade decisions/fills after the fact; blocks any future forensic P&L attribution | Phase 0 (golden-file capture) | Phase 6 (Execution Engine) — `execution` module's order/trade journal closes this by design | Small once Phase 6 starts; not retrofittable into the frozen scripts without touching frozen logic |
| TD-002 | `paper_trader.py`'s live-update path (`run_daily_update`, `fetch_current_prices`, `check_regime`) has zero automated test coverage | Medium | A regression in the live daily path would only surface in production, not CI | Phase 0 | Phase 6 — `paper` module is designed to replace this loop entirely; testing the legacy loop directly isn't worth building | None now; superseded, not fixed |
| TD-003 | `fetch_universe.py`, `download_data.py`, `factor_attribution.py` — zero test coverage (0% each) | Low | Network-fetch/research tools; not on the live-trading critical path, but silent breakage wouldn't be caught | Phase 0 | Phase 2 (Data Platform) — `data` module replaces `fetch_universe.py`/`download_data.py`'s job entirely | None now; superseded, not fixed |
| TD-004 | `My_terminal/trading_backtests/` (16-strategy suite) has no git repository, no characterization tests, no golden files, no CI — zero Phase 0 safety net | High (for that codebase specifically) | Any future work touching that suite has no regression protection at all — it is *less* protected today than AlgoTrader/ was before Phase 0 | Phase 0 (scope decision — user confirmed AlgoTrader/-only git scope) | Requires its own Phase 0 pass, not yet scheduled | Medium — same shape of work as this repo's Phase 0, ~1 session |
| TD-005 | 29 pre-existing ruff findings in the 6 frozen scripts (E402 ×17 deliberate encoding-guard pattern, E501 ×12 wide print lines) | Low | Cosmetic/style only, no correctness risk; currently suppressed via disclosed `pyproject.toml` per-file-ignores | Phase 0 | Phase 1, only if/when those specific lines are touched during strangler-fig extraction — never a standalone fix | Trivial, but blocked on "don't touch frozen files" until Phase 1 |
| TD-006 | 7 pre-existing mypy errors in `momentum_backtest.py`/`paper_trader.py` (Optional-handling in `walk_forward_test`; `--selftest`'s monkeypatch pattern reads as `attr-defined` to the type checker) | Low | No runtime impact — code is correct, type checker just can't see it | Phase 0 | Phase 1 — resolved naturally once `strategies`/`paper` modules are typed from day one; not worth annotating legacy scripts for | Small, deferred |
| TD-007 | ~~No GitHub remote / CI never run on a real runner~~ **Resolved (2026-07-14):** full history + tags pushed to `github.com/jinujon007/QuantOS`; CI run 29351406212 is the repo's **first green run**. The prediction was correct twice over — real-runner validation immediately exposed (a) `generate_inventory.py` exiting 1 on unclassified files since Phase 0, its exit code always swallowed locally, and (b) a Windows-vs-Linux path-separator divergence that made the inventory gate unpassable by construction (`str(Path.parent)` = `data\cache` vs `data/cache`; fixed with `as_posix()`) | ~~Medium~~ Resolved | Closed: CI now actually gates | Phase 0 | **Resolved** — remaining habit: push as part of every WP's done-definition | Done |
| TD-008 | Coverage is 25% overall — by design (TD-002/TD-003 explain the 0%-coverage files), no enforced floor yet | Low | Not a regression risk today; becomes one if new code is added without tests and coverage silently drifts lower | Phase 0 | Phase 1, once `quantos_core`'s real test pyramid exists and a floor makes sense to enforce | Policy decision, ~0 effort |
| TD-009 | Blueprint §3's literal repository tree omits two modules (`research`, `api`) that its own §5 module specs require | Low | Would have caused ambiguity again the next time someone scaffolds these modules from the Blueprint alone | WP-000, while resolving the chat-tree/Blueprint conflict | Resolved — placement decided in ADR-031 | Done |
| TD-010 | Import-boundary enforcement (Constitution Part II item 4; ADR-029 leaf enforcement for `experiments`/`tools`) is not mechanically enforced anywhere — no import-linter or equivalent exists | ~~Medium~~ **Resolved (WP-005, 2026-07-14)** | Closed: `tests/quantos_core/test_import_boundaries.py` mechanically enforces the ADR-032 matrix + forbidden-layer/frozen-script bans in the CI-blocking suite, with relative-import resolution, a package/matrix drift guard, and a scanner self-test | WP-001 (scoped out per Technical Review Board direction) | **Resolved — WP-005 delivered** (stdlib-ast native scanner; `import-linter` dependency rejected per ADR-032) | Done |
| TD-011 | ~~`pip install -e ".[dev]"` fails on flat-layout auto-discovery~~ **Resolved (2026-07-15):** `[tool.setuptools.packages.find] include = ["quantos_core*"]` added to `pyproject.toml`; `pip install -e ".[dev]"` verified working ("Successfully installed quantos-algotrader-0.1.0"). The documented lockfile-regeneration method now actually runs | ~~Medium~~ Resolved | Closed | WP-002 | **Resolved** — 2026-07-15 maintenance pass | Done |
| TD-012 | `tools/generate_inventory.py`'s "Module Scaffold (empty)" bucket is path-prefix-matched on `quantos_core/*`, not content-aware — it now mislabels `quantos_core/config/schema.py` and `quantos_core/config/loader.py` (real, tested WP-002 code) as empty scaffold | Low | Cosmetic — `INVENTORY.md` misdescribes real code as a stub; no functional impact, but the doc is misleading the moment anyone reads it after WP-002 | WP-002, while regenerating `INVENTORY.md` | Classifier fix (distinguish real files from `__init__.py`-only/`.gitkeep` stubs within `quantos_core/*`) — not scoped into WP-002; will keep recurring each WP that adds real module code until fixed | Small, needs its own WP or a fold-in to whichever WP next touches `tools/generate_inventory.py` |

| TD-013 | Pre-live hardening backlog from the 2026-07-14 adversarial review — **updated by the 2026-07-14 program audit:** (a) write-ahead order journaling — *partially closed:* `_record_or_log` now guarantees a journal failure can never mask a placed order (receipt returned, CRITICAL log with full evidence); the write-BEFORE-place intent row remains Phase 6; (b) `CsvCachePriceProvider` index validation — **closed** (non-positive close, duplicate dates, unsorted dates all raise typed `DataFetchError`, tests added); (c) `get_logger` last-caller-wins run_id bleed — open (use LoggerAdapter); (d) Zerodha `holdings()` ignores `t1_quantity` — open; (e) journal id scheme ceilings noted: `{:04d}` sorts wrong past 9999 entries/run, two engines sharing a run_id concurrently would collide (single-writer assumption, ~20 orders/week) | Medium (remaining items gated on live/Phase 6+ conditions) | Receipt-loss and corrupt-cache failure modes now covered; run_id bleed + t1 reconciliation still latent for live wiring | 2026-07-14 adversarial review; updated 2026-07-14 program audit | Phase 6 (write-ahead intent row, LoggerAdapter, reconciliation incl. t1_quantity) | Small-medium |
| TD-014 | Momentum boundary uses last ROW, not last valid price per ticker: one NaN on exactly the `(as_of − skip_months)` row silently excludes a fully-historied ticker from that week's ranking. Inherited verbatim from the frozen script (ADR-003 byte-parity contract; `test_strategy_parity.py` pins it), so fixing it is a strategy **version bump**, not a bug fix | Low (behavior is chosen-and-pinned, but it was chosen implicitly) | A ticker can drop out of the top-10 for a data-gap reason invisible in any log; direction unbiased but adds selection noise | 2026-07-14 program audit (correctness review, finding 11) | Decide at the Momentum v1.1 boundary (needs its own ADR + validation-clock restart per the freeze rule) | Small code, big process (version bump) |
| TD-015 | Venv carries ~145 packages beyond the 59-line lockfile: dead LLM stack (`vibe-trading-ai` 0.1.9, `langchain*`, `openai`) already marked "dead weight, candidate for removal" in CONTEXT.md, never removed. A fresh `pip install` from the lockfile would not reproduce the running venv, and unused packages widen the supply-chain surface | Low-Medium | Environment drift: works-on-this-venv ≠ works-from-lockfile; TD-011's editable-install fix is the natural moment to rebuild clean | 2026-07-14 program audit | Rebuild venv from `requirements-lock.txt` after TD-011 lands (one sitting, verify with full test suite) | Small |
| TD-016 | `data/paper_state.json` is git-tracked while the scheduled task mutates it daily — every session shows a dirty tree, and any `git checkout/restore/stash` silently rolls the live validation account back in time | Low | State-rollback foot-gun plus permanent working-tree noise; the trade audit trail (`paper_trades.csv`) is the real evidence file | 2026-07-14 program audit (ops review) | Decide: either commit state daily as part of the runner (auditable history) or gitignore the live file and commit dated snapshots; needs a one-line ADR either way | Trivial |

## Severity definitions

- **High** — affects an active live/paper-trading path, or leaves a whole codebase with no regression protection.
- **Medium** — affects correctness confidence or CI reliability, but not live capital or the currently-frozen strategies directly.
- **Low** — style/cosmetic, or fully superseded by a planned future module with no interim fix needed.
